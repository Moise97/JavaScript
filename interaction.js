// Alert

alert("Hello"); // this show a message and pauses script execution until 
                //the user presses OK


// Prompt

age = prompt("How old are you?", 20); // this shows a modal window with a text message,
                // an input for field for the visitor, and the buttons Ok, Cancel


// Confirm

let isBoss = confirm("Are you the boss?"); // the function confirm shows a modal window,
                // with a question and two buttons: Ok and Cancel

alert(isBoss); // the result is true if Ok is pressed and false otherwise 